---
/* eslint-disable */
export interface Props extends astroHTML.JSX.AnchorHTMLAttributes {}
/* eslint-enable */

const { href, class: className, color = 'text-ctp-red dark:text-ctp-pink', ...props } = Astro.props;
---

<!-- prettier-ignore -->
<a href={href} class:list={`${[className]} hover:underline ${color}`} {...props}><slot /></a>

---
import { Icon } from 'astro-icon/components';
---

<form class="flex flex-col gap-4" action="/api/contact/maintainer" method="POST">
  <div>
    <label for="name">Your name</label>
    <input type="text" id="name" name="name" placeholder="John Doe" required />
    <p>This can be your preferred name.</p>
  </div>

  <div>
    <label for="bungieName">Your Bungie Name</label>
    <input type="text" id="bungieName" name="bungieName" placeholder="Guardian#1234" required />
    <p>Your Destiny 2 Bungie Name, to check if you have the Parallel Program emblem.</p>
  </div>

  <div>
    <label for="link">Your link</label>
    <input type="url" id="link" name="link" placeholder="https://myawesomewebsite.com" required />
    <p>This can be your Bungie.net profile, your personal website, a GitHub profile, or anything, really.</p>
  </div>

  <div>
    <label for="resourceName">Resource name</label>
    <input type="text" id="resourceName" name="resourceName" placeholder="Destiny Item Manager" required />
    <p>The name of your resource.</p>
  </div>

  <div>
    <label for="resourceLink">Resource link</label>
    <input type="url" id="resourceLink" name="resourceLink" placeholder="https://twitter.com/ThisIsDIM" required />
    <p>This can either be a link to your resource's website or a GitHub repository link.</p>
  </div>

  <div>
    <label for="resourceIcon">Resource icon (URL) <span class="text-base font-normal">(optional)</span></label>
    <input type="url" id="resourceIcon" name="resourceIcon" placeholder="https://github.com/DestinyItemManager.png" />
    <p>A link to your resource's icon, if you have one. If your resource doesn't have one, this can be skipped.</p>
  </div>

  <div>
    <label for="resourceColor">Resource color</label>
    <input type="text" id="resourceColor" name="resourceColor" placeholder="#FFFFFF" required />
    <p>The HEX color of your resource.</p>
  </div>

  <div>
    <label for="resourceRole">Your role / contribution</label>
    <input type="text" id="resourceRole" name="resourceRole" placeholder="Developer" required />
    <p>Your role in the resource.</p>
  </div>

  <div>
    <label for="resourceContributors">Resource contributors <span class="text-base font-normal">(optional)</span></label>
    <textarea
      id="resourceContributors"
      name="resourceContributors"
      placeholder="John Doe#1234, Developer, https://twitter.com/johndoe
Jane Doe#4321, Contributor, https://github.com/janedoe"
      rows="4"></textarea>
    <p>
      Optionally, if your resource has any contributors, list them here. You must include their Bungie Names, their roles and their links.
    </p>
  </div>

  <div>
    <label for="contact"
      >Your means of communication, so we can reach back to you <span class="text-base font-normal">(optional)</span></label
    >
    <input type="text" id="contact" name="contact" placeholder="Email (hello@example.com), Discord tag (username), etc" />
    <p>This step is optional, but we can contact you in case we add your resource, or if we have questions regarding your resource.</p>
  </div>

  <div class="cf-turnstile" data-sitekey={import.meta.env.TURNSTILE_SITE_KEY}></div>

  <div>
    <button type="submit">
      <Icon name="mdi:alternate-email" />
      Submit
    </button>
  </div>
</form>

<style>
  label {
    @apply block pb-1 font-semibold;
  }

  input,
  textarea {
    @apply w-full rounded-md border-2 border-ctp-surface2 border-opacity-60 bg-ctp-base px-4 py-2 transition ease-out focus:border-ctp-red focus:outline-none focus:ring-0;
  }

  textarea + p {
    @apply -mt-1.5;
  }

  p {
    @apply pt-1 text-base opacity-80;
  }

  button {
    @apply flex flex-row items-center gap-2 rounded-lg bg-ctp-red p-3 text-center font-medium text-ctp-base transition ease-out hover:opacity-80;
  }
</style>

<div class="article-content prose dark:prose-invert">
  <slot />
</div>

<style is:global>
  .article-content {
    @apply text-xl;
    @apply self-center text-center;
    @apply flex flex-col;
    @apply pb-5;

    & a {
      text-decoration: none;
    }
  }
</style>

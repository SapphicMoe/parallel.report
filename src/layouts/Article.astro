<div class="article-content prose prose-invert">
  <slot />
</div>

<style is:global>
  .article-content {
    @apply text-xl;
    @apply self-center text-center;
    @apply flex flex-grow flex-col flex-wrap;
    @apply pb-5;

    & a {
      text-decoration: none;
    }
  }
</style>
